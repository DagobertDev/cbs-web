import { PositionConverterPipe } from './position-converter.pipe';

describe('PositionConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new PositionConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
